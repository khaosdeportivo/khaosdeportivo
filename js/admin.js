/*
 * El nucleo del admin se carga desde CDN en admin.html
 * (commit estable 0c971526acc7).
 * Este archivo local se mantiene solo por compatibilidad.
 */
console.log('[Khaos] admin.js local OK (nucleo en CDN)');
